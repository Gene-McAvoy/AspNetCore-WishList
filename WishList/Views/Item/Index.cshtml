@model List<WishList.Models.Item>
@{
}

<h1>Wishlist</h1>
<a asp-action="create">Add item</a>
<ul> 
    @foreach(var i in Model)
     {
        <li> i.Description 
            <a asp-action="delete"
               asp-route-id=i.Id>delete</a>
        </li>
     }
</ul>